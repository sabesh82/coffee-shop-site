import React from "react";
import LoginForm from "./LoginForm";

const page = () => {
  return (
    <section className="w-full min-h-screen flex items-center justify-center p-10">
      <div className="">
        <LoginForm />
      </div>
    </section>
  );
};

export default page;
